import 'mocha';
import { expect } from 'chai';
import { Libreria } from '../src/Libreria'
import { Playlist } from '../src/playlist'


describe ( 'Test para la clase abstracta' , () => {
    
    // constructor(private nombre:string, private canciones:Cancion [], 
    //     private generos:GenerosMusicales) {
    
    let playlist1 = new Playlist("myFavorite", )
    
    it("Test1", () => {
        expect()
    });
});